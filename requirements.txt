kagglehub
numpy
pandas
matplotlib
seaborn
opencv-python
tqdm
pyyaml

# Task-02: model training / inference
# Install torch + torchvision with CUDA wheels separately, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch
torchvision
ultralytics>=8.3
supervision
scipy

# Task-04: ByteTrack / BoT-SORT need linear-assignment.
# Ultralytics auto-installs this on first tracker call if missing, but
# pinning it here avoids the ~44s autoinstall on a clean machine.
lap>=0.5.12
